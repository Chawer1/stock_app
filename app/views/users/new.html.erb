<h1>Sign Up</h1>

<%= form_for @user do |f| %>
  <%= f.label :email %>
  <%= f.email_field :email %>

  <%= f.label :password_digest %>
  <%= f.password_field :password_digest %>

  <%= f.submit "Sign Up" %>
<% end %>